<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
    <variable
        name="user"
        type="com.edu.unab.mgads.henryf.storapp.model.entity.User" />
    </data>
    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="8dp"
        app:cardElevation="8dp"
        android:layout_margin="8dp">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <ImageView
            android:id="@+id/iv_user_picture"
            android:layout_width="140dp"
            android:layout_height="160dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/tv_name_user_item"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_margin="8dp"
            android:src="@drawable/ic_launcher_background"
            bind:image="@{user.urlPicture}"
            android:scaleType="centerCrop"
            />
        <TextView
            android:id="@+id/tv_name_user_item"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="0dp"
            app:layout_constraintTop_toBottomOf="@id/iv_user_picture"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/tv_document_user_item"
            android:textAlignment="center"
            android:text="@{user.name}"
            />
        <TextView
            android:id="@+id/tv_document_user_item"
            android:layout_margin="0dp"
            android:layout_width="0dp"
            android:textAlignment="center"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toTopOf="@id/tv_email_user_item"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_name_user_item"
            app:layout_constraintStart_toStartOf="parent"
            android:text="@{user.document}"
            />
        <TextView
            android:id="@+id/tv_email_user_item"
            android:layout_marginBottom="8dp"
            android:layout_width="0dp"
            android:textAlignment="center"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_document_user_item"
            app:layout_constraintStart_toStartOf="parent"
            android:text="@{user.email}"
            />


    </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>